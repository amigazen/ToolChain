AC demo / regression suite
==========================

Runnable suite emits **DejaGnu / GCC `.sum` style** results (PASS/FAIL +
Summary) — the usual format for C compiler test reporting.

Amiga (from `src`)
------------------
  make demo-self2
  demo-out/ac-self2/ac_tests

Or build and run in one step:

  make demo-run-self2

Also: `make demo-ac`, `make demo-self`, `make demo`.

What gets built
---------------
- `demo-out/<cc>/*.s` — compile-only smokes for each `test_*.c`
- `demo-out/<cc>/ac_tests` — linked DejaGnu driver (`/demo/ac_tests.c`)
- `demo-out/<cc>/cli/` — command-line option smoke outputs

New smokes this turn
--------------------
- `test_bss_arrays.c` — global `int[10]` / `char*[10]` must get `DS.b 40` (not 10)
- `test_ac_debug_brace.c` — documents the AC_DEBUG dangling-if brace rule
- `ac_tests` also runs runtime `bss/sizeof_*` expects

CLI options: `make demo-cli-self2` (also runs as part of `make demo-self2`).
See `cli_opts.txt`.

Example output
--------------
  PASS: smoke/global_answer
  PASS: literal/binary_or
  FAIL: some_test (got 0 want 1)
  ...
                === ac demo Summary ===

  # of expected passes            28
  # of unexpected failures        0
  # of untested testcases         2

Exit status 0 = all PASS; 1 = any FAIL.
