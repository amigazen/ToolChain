/* iso646.h - alternative operator spellings (C95 / C23)
 *
 * AC recognizes these as built-in keywords (aliases of && || ! & | ^ etc.).
 * This header exists for portable #include compatibility.
 */

#ifndef __ISO646_H
#define __ISO646_H

/* Keywords are built into the compiler; nothing else required here. */

#endif /* __ISO646_H */
